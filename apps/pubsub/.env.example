# PubSub Service Configuration
# Uses the SAME database as REST API

# Database - same connection as apps/api
DATABASE_URL=postgresql://app:app@localhost:5434/app

# JWT Secret - must match REST API
JWT_SECRET=your-jwt-secret-here

# WebSocket port
PUBSUB_PORT=4001
