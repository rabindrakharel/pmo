# ============================================================================
# VIEW TYPE MAPPING
# ============================================================================
# Maps fieldBusinessType to component-specific VIEW/DISPLAY rendering
# Version: 4.0.0
#
# ============================================================================
# PURPOSE & POSITION IN MAPPING CHAIN
# ============================================================================
#
# This file is STEP 2a in the field metadata pipeline:
#
#   Column Name (e.g., "budget_allocated_amt")
#         │
#         ▼
#   pattern-mapping.yaml
#   (column pattern → fieldBusinessType)
#         │
#         ▼
#   fieldBusinessType (e.g., "currency")
#         │
#         ├──────────────────────────────────────────┐
#         ▼                                          ▼
#   ┌─────────────────────────────────────┐   ┌─────────────────────────────┐
#   │  VIEW-TYPE-MAPPING.YAML (this file) │   │  edit-type-mapping.yaml     │
#   │  ─────────────────────────────────  │   │  ─────────────────────────  │
#   │  Defines how to DISPLAY the field   │   │  Defines how to EDIT field  │
#   │  (read-only rendering)              │   │  (input controls)           │
#   │                                     │   │                             │
#   │  Output: renderType, component,     │   │  Output: inputType,         │
#   │          behavior, style            │   │          behavior, style,   │
#   │                                     │   │          validation         │
#   └─────────────────────────────────────┘   └─────────────────────────────┘
#
# ============================================================================
# RENDERTYPE RULES (CRITICAL!)
# ============================================================================
#
# renderType determines HOW a field is rendered:
#
#   NATIVE TYPES (inline rendering, NO component field!):
#   ─────────────────────────────────────────────────────
#   - text         : Plain text display
#   - number       : Numeric display (with optional unit, precision)
#   - currency     : Currency display (with symbol, decimals, locale)
#   - percentage   : Percentage display (with decimals)
#   - date         : Date display (with format, locale)
#   - timestamp    : DateTime display (with format)
#   - time         : Time display
#   - duration     : Duration display (with unit: minutes, hours, seconds, ms)
#   - boolean      : Yes/No, True/False, Active/Inactive display
#   - filesize     : Human-readable file size (KB, MB, GB)
#   - badge        : Colored status/datalabel badge (with colorFromData)
#   - entityLink   : Single entity reference link (with displayField, linkToEntity)
#   - entityLinks  : Multiple entity reference links (with maxDisplay)
#   - tags         : Tag/chip display (with maxDisplay)
#   - file         : File link/preview
#   - image        : Image thumbnail
#   - avatar       : Avatar image (circular)
#   - color        : Color swatch (with showHex)
#   - icon         : Icon display
#   - json         : JSON preview (with collapsed)
#
#   All rendering is done INLINE by formatDataset() - no separate cell components.
#
# EXAMPLES:
#
#   CORRECT (Native type - no component):
#     entityListOfInstancesTable:
#       renderType: currency
#       style: { symbol: "$", decimals: 2 }
#
#   CORRECT (Badge for datalabel):
#     entityListOfInstancesTable:
#       renderType: badge
#       style: { colorFromData: true }
#
#   CORRECT (Entity link):
#     entityListOfInstancesTable:
#       renderType: entityLink
#       style: { displayField: name, linkToEntity: true }
#
# ============================================================================
# API OUTPUT STRUCTURE (v10.0.0)
# ============================================================================
#
# The API returns metadata with viewType and editType as top-level containers
# within each component. This separates VIEW and EDIT concerns cleanly:
#
#   {
#     "data": [...],
#     "metadata": {
#       "entityListOfInstancesTable": {
#         "viewType": {                          ← All VIEW metadata here
#           "budget_allocated_amt": {
#             "dtype": "float",
#             "label": "Budget Allocated",
#             "renderType": "currency",          ← renderType from this file
#             "behavior": { "visible": true, "sortable": true },
#             "style": { "symbol": "$", "decimals": 2 }
#           },
#           "dl__project_stage": {
#             "dtype": "str",
#             "label": "Project Stage",
#             "renderType": "badge",
#             "lookupField": "dl__project_stage", ← v12.0.0: For badge color lookup
#             "behavior": { "visible": true, "filterable": true },
#             "style": { "colorFromData": true }
#           }
#         },
#         "editType": {                          ← All EDIT metadata here
#           "budget_allocated_amt": {
#             "dtype": "float",
#             "label": "Budget Allocated",
#             "inputType": "number",             ← inputType from edit-type-mapping.yaml
#             "behavior": { "editable": true },
#             "style": {},
#             "validation": { "min": 0 }
#           }
#         }
#       }
#     }
#   }
#
# ============================================================================
# THREE CATEGORIES (Quick Reference)
# ============================================================================
#
#   behavior:   Interaction settings (visible, sortable, filterable, searchable)
#   style:      Visual presentation AND formatting (width, align, symbol, decimals, ...)
#   validation: Input constraints (edit-type-mapping.yaml only)
#
# ============================================================================
# HOW THIS FILE IS COMPOSED
# ============================================================================
#
# Each fieldBusinessType entry has:
#
#   fieldBusinessType:
#     dtype: <data_type>           # REQUIRED - Database/JS data type (ONLY root-level field)
#     inherit: <other_type>        # Optional - Inherit from another type
#
#     # Component-specific definitions (REQUIRED for each supported component):
#     entityListOfInstancesTable:
#       behavior: { visible, sortable, filterable, searchable }
#       style: { width, align, symbol, decimals, locale, ... }
#
#     entityInstanceFormContainer:
#       behavior: { visible, editable }
#       style: { size, showBar, monospace, ... }
#
#     kanbanView:
#       behavior: { visible }
#       style: { truncate, compact, ... }
#
#     gridView:
#       behavior: { visible }
#       style: { ... }
#
#     calendarView:
#       behavior: { visible }
#       style: { ... }
#
#     dagView:
#       behavior: { visible }
#       style: { ... }
#
# ============================================================================
# CRITICAL RULE: Root-level vs Component-level Fields
# ============================================================================
#
# !! IMPORTANT !! Only dtype and inherit belong at root level!
#
# CORRECT:
#   duration:
#     dtype: int
#     entityListOfInstancesTable: { <<: *table_default, style: { width: "100px", unit: minutes } }
#     entityInstanceFormContainer: { <<: *form_default, style: { unit: minutes } }
#
# WRONG:
#   duration:
#     dtype: int
#     renderType: duration       # <-- ERROR! Not root-level
#     width: "100px"             # <-- ERROR! Not root-level
#     entityListOfInstancesTable: { ... }
#
# All display/behavior fields are page-specific and belong INSIDE
# each component section (entityListOfInstancesTable, entityInstanceFormContainer, etc.)
#
# ============================================================================
# ROOT-LEVEL FIELDS (only these!)
# ============================================================================
#
# dtype (REQUIRED) - Data type:
#   - str, int, float, bool, uuid, date, timestamp, jsonb
#   - array[str], array[uuid]
#
# inherit - Copy all settings from another fieldBusinessType (optional)
#
# ============================================================================
# COMPONENT-LEVEL FIELDS (organized into behavior, style)
# ============================================================================
#
# BEHAVIOR - Interaction/visibility settings:
#   visible     - Whether field is shown (default: true for tables, varies for others)
#   sortable    - Whether column can be sorted (tables only)
#   filterable  - Whether field appears in filters (tables only)
#   searchable  - Whether field is included in text search
#   required    - Show required indicator (*) in display mode (default: false)
#
# STYLE - Visual presentation AND formatting:
#   width       - Column width: "auto", "80px", "120px", "200px", etc.
#   align       - Text alignment: left (default), center, right
#   symbol      - Currency symbol: "$", "€", etc.
#   decimals    - Number of decimal places: 0, 2, etc.
#   locale      - Locale for formatting: "en-CA", "fr-CA", etc.
#   truncate    - Max characters before truncation: 50, 80, etc.
#   monospace   - Use monospace font: true/false
#   bold        - Bold text: true/false
#   linkable    - Clickable link: true/false
#   linkToDetail- Link to entity detail page: true/false
#   colorFromData - Badge color from datalabel: true/false
#   showFlag    - Show country flag: true/false
#   compact     - Compact display mode: true/false
#   unit        - Unit for measurements: "minutes", "hours", "kg", etc.
#   emptyValue  - Display when value is null/empty: "—", "N/A", "(none)", etc.
#   helpText    - Tooltip text shown on hover (contextual guidance)
#
# NOTE: validation rules are in edit-type-mapping.yaml only (for input controls)
#
# ============================================================================
# COMPONENT ARCHITECTURE: PAGE-LEVEL CONTAINERS + INLINE RENDERING
# ============================================================================
#
# This system uses PAGE-LEVEL containers with INLINE rendering (no separate
# field-level components):
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │  PAGE-LEVEL COMPONENTS (Containers)                                      │
# │  ═══════════════════════════════════                                     │
# │  These are the KEYS in this YAML file. They define WHERE fields render.  │
# │  They are React container components that orchestrate multiple fields.   │
# │                                                                          │
# │  Actual files:                                                           │
# │    - EntityListOfInstancesTable.tsx      (tables)                                   │
# │    - EntityInstanceFormContainer.tsx  (detail/form views)                        │
# │    - KanbanView.tsx           (kanban boards)                            │
# │    - CalendarView.tsx         (calendar events)                          │
# │    - DAGVisualizer.tsx        (workflow DAG)                             │
# └─────────────────────────────────────────────────────────────────────────┘
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │  INLINE RENDERING (No separate field components)                         │
# │  ═══════════════════════════════════════════════                         │
# │  Field rendering happens INLINE within page-level containers using:      │
# │    - formatDataset() for view mode (returns display strings + styles)    │
# │    - renderEditModeFromMetadata() for edit mode (returns input elements) │
# │                                                                          │
# │  The `component` key in this YAML is a descriptive hint only.            │
# │  Actual rendering is based on renderType, inputType, and style options.  │
# └─────────────────────────────────────────────────────────────────────────┘
#
# Example - how datalabel fields are rendered:
#
#   datalabel:
#     dtype: str
#     entityListOfInstancesTable:       ←── PAGE-LEVEL (container for table columns)
#       style: { colorFromData: true }   ←── Inline badge rendering
#     entityInstanceFormContainer:   ←── PAGE-LEVEL (container for detail view)
#       viz_container: DAGVisualizer     ←── Triggers DAGVisualizer component
#     dagView:               ←── PAGE-LEVEL (container for workflow DAG)
#       behavior: { visible: true }      ←── Node rendered inline by DAGVisualizer
#
# ============================================================================
# PAGE-LEVEL COMPONENTS (6 containers)
# ============================================================================
#
# These are the section KEYS under each fieldBusinessType:
#
# ┌────────────────────┬────────────────────────────────────────────────────┐
# │ Component Key      │ Description                                        │
# ├────────────────────┼────────────────────────────────────────────────────┤
# │ entityListOfInstancesTable    │ Main entity list table (EntityListOfInstancesTable.tsx)       │
# │                    │ Renders sortable/filterable columns with data      │
# │                    │ Used on: EntityListOfInstancesPage                 │
# │                    │ Layout: Tabular rows and columns                   │
# ├────────────────────┼────────────────────────────────────────────────────┤
# │ entityInstanceFormContainer│ Entity detail/form view (EntityInstanceFormContainer.tsx)  │
# │                    │ Renders Notion-style property list                 │
# │                    │ Used on: EntitySpecificInstancePage                │
# │                    │ Layout: Vertical key-value pairs                   │
# ├────────────────────┼────────────────────────────────────────────────────┤
# │ kanbanView         │ Kanban board cards (KanbanView.tsx)                │
# │                    │ Renders fields on draggable cards                  │
# │                    │ Used on: EntityListOfInstancesPage (kanban mode)   │
# │                    │ Layout: Cards grouped by status column             │
# ├────────────────────┼────────────────────────────────────────────────────┤
# │ gridView           │ Card grid view (GridView.tsx)                      │
# │                    │ Renders fields on grid cards                       │
# │                    │ Used on: EntityListOfInstancesPage (grid mode)     │
# │                    │ Layout: Responsive card grid                       │
# ├────────────────────┼────────────────────────────────────────────────────┤
# │ calendarView       │ Calendar event display (CalendarView.tsx)          │
# │                    │ Renders fields in calendar events                  │
# │                    │ Used on: EntityListOfInstancesPage (calendar mode) │
# │                    │ Layout: Monthly/weekly calendar with events        │
# ├────────────────────┼────────────────────────────────────────────────────┤
# │ dagView            │ DAG workflow visualizer (DAGVisualizer.tsx)        │
# │                    │ Renders fields as nodes in directed acyclic graph  │
# │                    │ Used on: Workflow pages, datalabel hierarchies     │
# │                    │ Layout: Connected nodes showing progression        │
# └────────────────────┴────────────────────────────────────────────────────┘
#
# ============================================================================
# ACTUAL RENDERING ARCHITECTURE (v8.0.0)
# ============================================================================
#
# IMPORTANT: This project does NOT use separate cell/field components.
# Rendering is done INLINE within page-level containers using formatters.
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ VIEW MODE RENDERING (format-at-read pattern)                            │
# ├─────────────────────────────────────────────────────────────────────────┤
# │                                                                          │
# │ 1. API returns raw data + metadata (renderType, inputType, style)       │
# │ 2. React Query's `select` calls formatDataset()                         │
# │ 3. Returns FormattedRow with:                                           │
# │    - display[key]: Pre-formatted string ("$50,000.00")                  │
# │    - styles[key]: Tailwind classes ("bg-green-100 text-green-700")      │
# │ 4. Page containers render inline using display/styles                   │
# │                                                                          │
# │ Source: apps/web/src/lib/formatters/datasetFormatter.ts                 │
# └─────────────────────────────────────────────────────────────────────────┘
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ EDIT MODE RENDERING (renderEditModeFromMetadata)                        │
# ├─────────────────────────────────────────────────────────────────────────┤
# │                                                                          │
# │ Uses inputType from metadata to render appropriate input:               │
# │                                                                          │
# │   inputType       → Renders                                             │
# │   ─────────────────────────────────────────────────────                 │
# │   text            → DebouncedInput (type="text")                        │
# │   number          → DebouncedInput (type="number")                      │
# │   currency        → DebouncedInput (type="number" step="0.01")          │
# │   textarea        → DebouncedTextarea                                   │
# │   date            → <input type="date">                                 │
# │   datetime        → <input type="datetime-local">                       │
# │   checkbox        → <input type="checkbox">                             │
# │   component       → Component from `component` field (DataLabelSelect)  │
# │   multiselect     → MultiSelect                                         │
# │                                                                          │
# │ Source: apps/web/src/lib/frontEndFormatterService.tsx                   │
# └─────────────────────────────────────────────────────────────────────────┘
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ ACTUAL SHARED UI COMPONENTS (apps/web/src/components/shared/ui/)        │
# ├─────────────────────────────────────────────────────────────────────────┤
# │                                                                          │
# │ INPUT COMPONENTS                                                         │
# │   DebouncedInput      - Text/number input with debounce                 │
# │   DebouncedTextarea   - Textarea with debounce                          │
# │   BadgeDropdownSelect - Select with colored options (datalabels)        │
# │   Select              - Basic select dropdown                           │
# │   MultiSelect         - Multi-select dropdown                           │
# │   DataLabelSelect     - Datalabel-specific select                       │
# │   EntityInstanceNameLookup - Searchable entity reference select          │
# │   EditableTags        - Tag input/editing                                │
# │                                                                          │
# │ DISPLAY COMPONENTS                                                       │
# │   KanbanView          - Kanban board (page-level)                       │
# │   CalendarView        - Calendar display (page-level)                   │
# │   TreeView            - Hierarchical tree display                       │
# │   DateRangeVisualizer - Date range bar visualization                    │
# │                                                                          │
# │ WORKFLOW                                                                 │
# │   DAGVisualizer       - DAG workflow visualization (page-level)         │
# │                         Source: apps/web/src/components/workflow/       │
# │                                                                          │
# └─────────────────────────────────────────────────────────────────────────┘
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ SPECIAL VISUALIZATION CONTAINERS (EntityInstanceFormContainer_viz_container)    │
# ├─────────────────────────────────────────────────────────────────────────┤
# │                                                                          │
# │ For datalabel fields with DAG structure, EntityInstanceFormContainer checks:    │
# │   if (field.EntityInstanceFormContainer_viz_container === 'DAGVisualizer')      │
# │                                                                          │
# │ Valid values:                                                            │
# │   DAGVisualizer       - Renders DAG workflow for datalabel stages       │
# │   MetadataTable       - Renders key-value metadata table                │
# │   ProgressBar         - Renders progress bar visualization              │
# │   DateRangeVisualizer - Renders date range bar                          │
# │                                                                          │
# └─────────────────────────────────────────────────────────────────────────┘
#
# ============================================================================
# YAML ANCHORS & ALIASES
# ============================================================================
#
# This file uses YAML anchors (&) and aliases (*) to reduce duplication:
#
#   &table_default    - Default settings for table columns
#   &form_default     - Default settings for form fields
#   &hidden           - Hidden field settings
#
# Usage:
#   entityListOfInstancesTable: { <<: *table_default, style: { width: "200px" } }
#   entityInstanceFormContainer: { <<: *hidden }
#
# ============================================================================
# NOTE ON COMPONENT FIELD
# ============================================================================
#
# The `component` field in this YAML is a DESCRIPTIVE HINT, not an actual React
# component name. The frontend does NOT use this field for rendering.
#
# Actual rendering is determined by:
#   - View mode: renderType + style options → formatDataset() inline rendering
#   - Edit mode: inputType + style options → renderEditModeFromMetadata()
#
# The component field is included in API metadata for documentation purposes
# and potential future use, but current rendering is fully inline.
#
# ============================================================================
# EXAMPLES
# ============================================================================
#
# Simple text field:
#   name:
#     dtype: str
#     entityListOfInstancesTable: { <<: *table_default, style: { width: "250px" } }
#     entityInstanceFormContainer: { <<: *form_default }
#
# Currency field:
#   currency:
#     dtype: float
#     entityListOfInstancesTable: { <<: *table_default, style: { align: right, symbol: "$", decimals: 2 } }
#     entityInstanceFormContainer: { <<: *form_default }
#
# Datalabel with DAG visualization:
#   datalabel:
#     dtype: str
#     entityListOfInstancesTable: { <<: *table_default, style: { colorFromData: true } }
#     entityInstanceFormContainer: { <<: *form_default, viz_container: DAGVisualizer }
#
# Inherited type:
#   title:
#     dtype: str
#     inherit: name
#
# ============================================================================
# Uses YAML anchors (&) and aliases (*) to reduce duplication

# ============================================================================
# COMPONENT DEFAULTS (inherited unless overridden)
# ============================================================================
defaults:
  # entityListOfInstancesTable defaults
  table: &table_default
    visible: true
    sortable: true
    filterable: true
    searchable: false
    width: auto
    align: left

  # entityInstanceFormContainer defaults
  form: &form_default
    visible: true
    editable: true

  # kanbanView defaults
  kanban: &kanban_default
    visible: false

  # gridView defaults
  grid: &grid_default
    visible: false

  # calendarView defaults
  calendar: &calendar_default
    visible: false

  # dagView defaults
  dag: &dag_default
    visible: false

  # Hidden field (for all views)
  hidden: &hidden
    visible: false
    sortable: false
    filterable: false
    searchable: false

# ============================================================================
# FIELD BUSINESS TYPE DEFINITIONS
# ============================================================================
fieldBusinessTypes:

  # --------------------------------------------------------------------------
  # IDENTITY & CORE
  # --------------------------------------------------------------------------
  uuid:
    dtype: uuid
    entityListOfInstancesTable:
      renderType: text
      behavior: { visible: false, sortable: false, filterable: false }
    entityInstanceFormContainer:
      renderType: text
      behavior: { visible: true }
      style: { monospace: true, truncate: 8 }
    kanbanView:
      renderType: text
      behavior: { visible: false }
    gridView:
      renderType: text
      behavior: { visible: false }

  code:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "140px", monospace: true, emptyValue: "—" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { monospace: true, emptyValue: "—", helpText: "Unique identifier code (e.g., PROJ-001)" }
    kanbanView:
      renderType: text
      behavior: { visible: true }
      style: { monospace: true }
    gridView:
      renderType: text
      behavior: { visible: true }
      style: { monospace: true }

  name:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      behavior: { visible: true, sortable: true, filterable: true, searchable: true, required: true }
      style: { width: "250px", bold: true, linkToDetail: true, emptyValue: "(untitled)" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { required: true }
      style: { size: lg, emptyValue: "(untitled)", helpText: "The display name for this record" }
    kanbanView:
      renderType: text
      behavior: { visible: true, required: true }
      style: { truncate: 50, emptyValue: "(untitled)" }
    gridView:
      renderType: text
      behavior: { visible: true }
    calendarView:
      renderType: text
      behavior: { visible: true }

  title:
    dtype: str
    inherit: name

  subject:
    dtype: str
    inherit: name

  description:
    dtype: str
    entityListOfInstancesTable:
      <<: *hidden
      renderType: text
      style: { truncate: 100 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { multiline: true }
    kanbanView:
      renderType: text
      behavior: { visible: true }
      style: { truncate: 80 }
    gridView:
      renderType: text
      behavior: { visible: true }
      style: { truncate: 80 }

  summary:
    dtype: str
    inherit: description

  # --------------------------------------------------------------------------
  # CONTACT FIELDS
  # --------------------------------------------------------------------------
  email:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "200px", linkable: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { linkable: true }
    kanbanView: *hidden
    gridView:
      renderType: text
      behavior: { visible: true }
      style: { linkable: true }

  phone:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "140px", linkable: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { countryCode: CA }
    kanbanView: *hidden
    gridView:
      renderType: text
      behavior: { visible: true }
      style: { linkable: true }

  url:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "200px", truncate: true, linkable: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { linkable: true }
    kanbanView: *hidden
    gridView:
      renderType: text
      behavior: { visible: true }
      style: { linkable: true }

  # --------------------------------------------------------------------------
  # ADDRESS FIELDS
  # --------------------------------------------------------------------------
  address:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "250px", singleLine: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { multiline: true }
    kanbanView: *hidden
    gridView:
      renderType: text
      behavior: { visible: true }

  # address_line, street, city, state, province → consolidated to 'text' type
  # See pattern-mapping.yaml for patterns that map to 'text'

  country:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "120px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView:
      renderType: text
      behavior: { visible: true }

  postal_code:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "100px", monospace: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { country: CA }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # CURRENCY FIELDS
  # --------------------------------------------------------------------------
  currency:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: currency
      style: { align: right, width: "140px", symbol: "$", decimals: 2, locale: en-CA, emptyValue: "$0.00" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: currency
      style: { symbol: "$", decimals: 2, emptyValue: "$0.00", helpText: "Amount in Canadian dollars" }
    kanbanView:
      renderType: currency
      behavior: { visible: true }
      style: { compact: true, emptyValue: "$0" }
    gridView:
      renderType: currency
      behavior: { visible: true }

  currency_cents:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: currency
      style: { align: right, width: "120px", symbol: "$", locale: en-CA, cents: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: currency
      style: { cents: true }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # PERCENTAGE FIELDS
  # --------------------------------------------------------------------------
  percentage:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: percentage
      style: { align: right, width: "100px", decimals: 0 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: percentage
      style: { showBar: true }
    kanbanView:
      renderType: percentage
      behavior: { visible: true }
    gridView:
      renderType: percentage
      behavior: { visible: true }

  # --------------------------------------------------------------------------
  # DATE & TIME FIELDS
  # --------------------------------------------------------------------------
  date:
    dtype: date
    entityListOfInstancesTable:
      <<: *table_default
      renderType: date
      style: { width: "120px", format: short, locale: en-CA }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: date
      style: { format: medium }
    kanbanView:
      renderType: date
      behavior: { visible: true }
    gridView:
      renderType: date
      behavior: { visible: true }
    calendarView:
      renderType: date
      behavior: { visible: true }

  timestamp:
    dtype: timestamp
    entityListOfInstancesTable:
      <<: *table_default
      renderType: timestamp
      style: { width: "160px", format: datetime }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: timestamp
    kanbanView: *hidden
    gridView: *hidden
    calendarView:
      renderType: timestamp
      behavior: { visible: true }

  timestamp_readonly:
    dtype: timestamp
    entityListOfInstancesTable:
      <<: *table_default
      renderType: timestamp
      behavior: { visible: true, sortable: true, filterable: false }
      style: { width: "140px", format: relative }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: timestamp
      style: { showAbsolute: true, format: relative }
    kanbanView:
      renderType: timestamp
      behavior: { visible: true }
      style: { format: relative }
    gridView:
      renderType: timestamp
      behavior: { visible: true }
      style: { format: relative }

  time:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: time
      style: { width: "100px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: time
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # DURATION FIELDS
  # --------------------------------------------------------------------------
  duration:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: duration
      style: { width: "100px", unit: minutes }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: duration
      style: { unit: minutes }
    kanbanView:
      renderType: duration
      behavior: { visible: true }
    gridView:
      renderType: duration
      behavior: { visible: true }
    calendarView:
      renderType: duration
      behavior: { visible: true }

  duration_minutes:
    dtype: int
    inherit: duration

  duration_hours:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: duration
      style: { width: "100px", unit: hours }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: duration
      style: { unit: hours }
    kanbanView: *hidden
    gridView: *hidden

  duration_seconds:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: duration
      style: { width: "100px", unit: seconds }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: duration
      style: { unit: seconds }
    kanbanView: *hidden
    gridView: *hidden

  duration_ms:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: duration
      style: { width: "100px", unit: ms }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: duration
      style: { unit: ms }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # BOOLEAN FIELDS
  # --------------------------------------------------------------------------
  boolean:
    dtype: bool
    entityListOfInstancesTable:
      <<: *table_default
      renderType: boolean
      style: { align: center, width: "80px", trueLabel: "Yes", falseLabel: "No", trueColor: green, falseColor: gray }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: boolean
    kanbanView:
      renderType: boolean
      behavior: { visible: true }
    gridView:
      renderType: boolean
      behavior: { visible: true }

  status_flag:
    dtype: bool
    entityListOfInstancesTable:
      <<: *table_default
      renderType: boolean
      style: { align: center, width: "100px", trueLabel: Active, falseLabel: Inactive, trueColor: green, falseColor: red }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: boolean
      style: { trueLabel: Active, falseLabel: Inactive }
    kanbanView:
      renderType: boolean
      behavior: { visible: true }
      style: { trueColor: green, falseColor: red }
    gridView:
      renderType: boolean
      behavior: { visible: true }
      style: { trueColor: green, falseColor: red }

  # --------------------------------------------------------------------------
  # DATALABEL & ENTITY REFERENCE FIELDS
  # --------------------------------------------------------------------------
  datalabel:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: badge
      style: { width: "140px", colorFromData: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: badge
      style: { showHierarchy: false }
    kanbanView:
      renderType: badge
      behavior: { visible: true }
    gridView:
      renderType: badge
      behavior: { visible: true }
    calendarView:
      renderType: badge
      behavior: { visible: true }
    dagView:
      renderType: badge
      behavior: { visible: true }

  # DAG datalabels - hierarchical stage/state/status with parent-child relations
  # Matched by: dl__*_stage, dl__*_state, dl__*_status
  # Uses DAGVisualizer component for entityInstanceFormContainer view
  datalabel_dag:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: badge
      style: { width: "140px", colorFromData: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: component
      component: DAGVisualizer
      style: { showHierarchy: true, interactive: false }
    kanbanView:
      renderType: badge
      behavior: { visible: true }
    gridView:
      renderType: badge
      behavior: { visible: true }
    calendarView:
      renderType: badge
      behavior: { visible: true }
    dagView:
      renderType: badge
      behavior: { visible: true }

  # Entity reference fields - single UUID reference to another entity
  # View mode: renders EntityInstanceName component (resolves UUID to name via ref_data_entityInstance)
  # Edit mode: renders EntityInstanceNameSelect component with lookupSourceTable: entityInstance
  entityInstance_Id:
    dtype: uuid
    entityListOfInstancesTable:
      renderType: component
      component: EntityInstanceName
      behavior: { visible: true, sortable: false, filterable: false, searchable: false }
      style: { displayField: name, linkToEntity: true }
    entityInstanceFormContainer:
      renderType: component
      component: EntityInstanceName
      behavior: { visible: true }
      style: { displayField: name }
    kanbanView:
      renderType: component
      component: EntityInstanceName
      behavior: { visible: true }
    gridView:
      renderType: component
      component: EntityInstanceName
      behavior: { visible: true }

  # Entity reference fields - array of UUIDs referencing multiple entities
  # View mode: renders EntityInstanceNames component (resolves UUIDs to names, displays as chip list)
  # Edit mode: renders EntityInstanceNameMultiSelect component with lookupSourceTable: entityInstance
  entityInstance_Ids:
    dtype: array[uuid]
    entityListOfInstancesTable:
      renderType: component
      component: EntityInstanceNames
      behavior: { visible: true, sortable: false, filterable: false, searchable: false }
      style: { maxDisplay: 3 }
    entityInstanceFormContainer:
      renderType: component
      component: EntityInstanceNames
      behavior: { visible: true }
      style: { displayField: name }
    kanbanView:
      renderType: component
      component: EntityInstanceNames
      behavior: { visible: true }
      style: { max: 3 }
    gridView:
      renderType: component
      component: EntityInstanceNames
      behavior: { visible: true }

  # --------------------------------------------------------------------------
  # QUANTITY & NUMBER FIELDS
  # --------------------------------------------------------------------------
  quantity:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { width: "100px", align: right }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
    kanbanView: *hidden
    gridView:
      renderType: number
      behavior: { visible: true }

  count:
    dtype: int
    inherit: quantity

  number:
    dtype: int
    inherit: quantity

  sort_order:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { width: "80px", align: center }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # MEASUREMENT FIELDS
  # --------------------------------------------------------------------------
  area_sqft:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { align: right, width: "120px", unit: "sq ft" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
      style: { unit: "sq ft" }
    kanbanView: *hidden
    gridView: *hidden

  volume_m3:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { align: right, width: "120px", unit: "m³" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
      style: { unit: "m³" }
    kanbanView: *hidden
    gridView: *hidden

  dimension_cm:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "120px", unit: "cm" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { unit: "cm" }
    kanbanView: *hidden
    gridView: *hidden

  weight_kg:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { align: right, width: "100px", unit: kg }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
      style: { unit: kg }
    kanbanView: *hidden
    gridView: *hidden

  file_size:
    dtype: int
    entityListOfInstancesTable:
      <<: *table_default
      renderType: filesize
      style: { width: "100px", align: right }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: filesize
    kanbanView: *hidden
    gridView: *hidden

  capacity:
    dtype: int
    inherit: quantity

  # --------------------------------------------------------------------------
  # RATING & SCORE FIELDS
  # --------------------------------------------------------------------------
  rating:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { width: "120px", max: 5, suffix: "/5" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
      style: { max: 5, suffix: "/5" }
    kanbanView:
      renderType: number
      behavior: { visible: true }
    gridView:
      renderType: number
      behavior: { visible: true }

  score:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { width: "100px", align: right }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # COORDINATES & GEOLOCATION
  # --------------------------------------------------------------------------
  latitude:
    dtype: float
    entityListOfInstancesTable:
      <<: *table_default
      renderType: number
      style: { width: "120px", precision: 6 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
      style: { precision: 6 }
    kanbanView: *hidden
    gridView: *hidden

  longitude:
    dtype: float
    inherit: latitude

  geolocation:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { showMap: true }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # TEXT CONTENT FIELDS
  # --------------------------------------------------------------------------
  # notes, instructions → consolidated to 'text_long' type
  # See pattern-mapping.yaml for patterns that map to 'text_long'

  text_long:
    dtype: str
    entityListOfInstancesTable:
      <<: *hidden
      renderType: text
      style: { truncate: 100 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { rows: 4, multiline: true }
    kanbanView: *hidden
    gridView: *hidden

  rich_text:
    dtype: str
    entityListOfInstancesTable:
      <<: *hidden
      renderType: text
      style: { truncate: 100 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { multiline: true }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # JSON & STRUCTURED DATA
  # --------------------------------------------------------------------------
  json:
    dtype: jsonb
    entityListOfInstancesTable:
      <<: *hidden
      renderType: json
      style: { collapsed: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: component
      component: MetadataTable
      style: { collapsed: false }
    kanbanView: *hidden
    gridView: *hidden

  json_schema:
    dtype: jsonb
    inherit: json

  tags:
    dtype: array[str]
    entityListOfInstancesTable:
      <<: *hidden
      renderType: tags
      style: { maxDisplay: 3 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: tags
    kanbanView:
      renderType: tags
      behavior: { visible: true }
      style: { max: 2 }
    gridView:
      renderType: tags
      behavior: { visible: true }

  # --------------------------------------------------------------------------
  # FILE & MEDIA FIELDS
  # --------------------------------------------------------------------------
  file:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: file
      style: { width: "150px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: file
    kanbanView: *hidden
    gridView: *hidden

  image:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: image
      style: { width: "80px", size: thumbnail }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: image
      style: { preview: true }
    kanbanView:
      renderType: image
      behavior: { visible: true }
    gridView:
      renderType: image
      behavior: { visible: true }

  avatar:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: avatar
      style: { align: center, width: "60px", size: sm }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: avatar
      style: { size: lg }
    kanbanView:
      renderType: avatar
      behavior: { visible: true }
      style: { size: sm }
    gridView:
      renderType: avatar
      behavior: { visible: true }
      style: { size: md }

  signature:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: image
      style: { type: signature }
    kanbanView: *hidden
    gridView: *hidden

  video:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: file
      style: { type: video }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # S3/STORAGE FIELDS
  # --------------------------------------------------------------------------
  s3_bucket:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
    kanbanView: *hidden
    gridView: *hidden

  s3_key:
    dtype: str
    inherit: s3_bucket

  s3_url:
    dtype: str
    inherit: url

  s3_region:
    dtype: str
    inherit: s3_bucket

  hash:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
      style: { monospace: true }
    kanbanView: *hidden
    gridView: *hidden

  mime_type:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "120px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
    kanbanView: *hidden
    gridView: *hidden

  file_extension:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "80px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # BARCODE & SCANNING
  # --------------------------------------------------------------------------
  barcode:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "150px", monospace: true, type: code128 }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { monospace: true }
    kanbanView: *hidden
    gridView:
      renderType: text
      behavior: { visible: true }
      style: { monospace: true }

  sku:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "120px", monospace: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView: *hidden

  upc:
    dtype: str
    inherit: sku

  # --------------------------------------------------------------------------
  # COLOR
  # --------------------------------------------------------------------------
  color:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: color
      style: { width: "100px", showHex: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: color
    kanbanView:
      renderType: color
      behavior: { visible: true }
    gridView:
      renderType: color
      behavior: { visible: true }

  # --------------------------------------------------------------------------
  # STATUS & STATE FIELDS
  # --------------------------------------------------------------------------
  status:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: badge
      style: { width: "120px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: badge
    kanbanView:
      renderType: badge
      behavior: { visible: true }
    gridView:
      renderType: badge
      behavior: { visible: true }

  state_workflow:
    dtype: str
    inherit: status

  stage:
    dtype: str
    inherit: datalabel

  # --------------------------------------------------------------------------
  # CATEGORY & TYPE FIELDS
  # --------------------------------------------------------------------------
  type:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "120px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView: *hidden

  category:
    dtype: str
    inherit: type

  # --------------------------------------------------------------------------
  # IDENTIFICATION NUMBERS
  # --------------------------------------------------------------------------
  sensitive_id:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      style: { mask: "***-***-***" }
    kanbanView: *hidden
    gridView: *hidden

  tax_id:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "140px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView: *hidden

  reference_number:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "140px", monospace: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView: *hidden

  invoice_number:
    dtype: str
    inherit: reference_number

  order_number:
    dtype: str
    inherit: reference_number

  po_number:
    dtype: str
    inherit: reference_number

  tracking_number:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "160px", linkable: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # AUTHENTICATION & SECURITY
  # --------------------------------------------------------------------------
  username:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "140px" }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView: *hidden
    gridView: *hidden

  password_hash:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer: *hidden
    kanbanView: *hidden
    gridView: *hidden

  token:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
      style: { monospace: true }
    kanbanView: *hidden
    gridView: *hidden

  ip_address:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
      style: { width: "140px", monospace: true }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
    kanbanView: *hidden
    gridView: *hidden

  user_agent:
    dtype: str
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
      behavior: { editable: false }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # UI FIELDS
  # --------------------------------------------------------------------------
  icon:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: icon
      style: { width: "80px", align: center }
    entityInstanceFormContainer:
      <<: *form_default
      renderType: icon
    kanbanView: *hidden
    gridView: *hidden

  label:
    dtype: str
    inherit: name

  # --------------------------------------------------------------------------
  # SYSTEM FIELDS
  # --------------------------------------------------------------------------
  version:
    dtype: int
    entityListOfInstancesTable: *hidden
    entityInstanceFormContainer:
      <<: *form_default
      renderType: number
      behavior: { editable: false }
      style: { prefix: v }
    kanbanView: *hidden
    gridView: *hidden

  # --------------------------------------------------------------------------
  # DEFAULT TEXT
  # --------------------------------------------------------------------------
  text:
    dtype: str
    entityListOfInstancesTable:
      <<: *table_default
      renderType: text
    entityInstanceFormContainer:
      <<: *form_default
      renderType: text
    kanbanView:
      renderType: text
      behavior: { visible: true }
    gridView:
      renderType: text
      behavior: { visible: true }

  # --------------------------------------------------------------------------
  # SYSTEM INTERNAL FIELDS (hidden from UI)
  # --------------------------------------------------------------------------
  # These fields are for internal system use only (from_ts, to_ts, active_flag)
  # They use *hidden anchor to hide from all views

  systemInternal_ts:
    dtype: timestamp
    entityListOfInstancesTable:
      <<: *hidden
      renderType: timestamp
    entityInstanceFormContainer:
      <<: *hidden
      renderType: timestamp
    kanbanView:
      <<: *hidden
    gridView:
      <<: *hidden
    calendarView:
      <<: *hidden
    dagView:
      <<: *hidden

  systemInternal_flag:
    dtype: bool
    entityListOfInstancesTable:
      <<: *hidden
      renderType: boolean
    entityInstanceFormContainer:
      <<: *hidden
      renderType: boolean
    kanbanView:
      <<: *hidden
    gridView:
      <<: *hidden
    calendarView:
      <<: *hidden
    dagView:
      <<: *hidden
